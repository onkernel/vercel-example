{
  "name": "agent",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "logs": "kernel logs browser-tool -f",
    "kernel:deploy": "dotenv -- sh -c 'export KERNEL_API_KEY=$KERNEL_API_KEY && kernel deploy index.ts -e ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.12.4",
  "dependencies": {
    "@onkernel/cu-playwright": "^0.1.2",
    "@onkernel/sdk": "^0.7.1",
    "playwright": "^1.54.0",
    "zod": "^4.0.2"
  },
  "devDependencies": {
    "dotenv-cli": "^8.0.0"
  }
}
